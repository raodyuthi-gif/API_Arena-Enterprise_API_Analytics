apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "api-analytics.fullname" . }}-redis
  labels:
    {{- include "api-analytics.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "api-analytics.fullname" . }}-redis
  template:
    metadata:
      labels:
        app: {{ include "api-analytics.fullname" . }}-redis
    spec:
      containers:
        - name: redis
          image: {{ .Values.redis.image }}
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-analytics.fullname" . }}-redis
  labels:
    {{- include "api-analytics.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ include "api-analytics.fullname" . }}-redis
  ports:
    - port: 6379
